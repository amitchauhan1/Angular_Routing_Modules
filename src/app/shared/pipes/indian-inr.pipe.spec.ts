import { IndianINRPipe } from './indian-inr.pipe';

describe('IndianINRPipe', () => {
  it('create an instance', () => {
    const pipe = new IndianINRPipe();
    expect(pipe).toBeTruthy();
  });
});
